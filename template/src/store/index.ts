import Vue from 'vue';
import Vuex from 'vuex';

Vue.use(Vuex);

// Use vuex-module-decorators to add modules to the store
export default new Vuex.Store({
  state: { },
  mutations: { },
  actions: { },
  modules: { }
});
